# ─────────────────────────────────────────────────────────────────────────────
# Automated Windows Credential Audit Tool — Python Dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core extraction (preferred backend) ──────────────────────────────────────
# Impacket provides the most accurate SAM + NTDS hash extraction
impacket>=0.11.0

# ── Fallback extraction backend ──────────────────────────────────────────────
# Used when Impacket is unavailable — pure Python registry parsing
python-registry>=1.3.1

# ── Cryptography for native extraction fallback ───────────────────────────────
# AES-128-CBC decryption of SAM hive hashes (Vista+ revision 3)
pycryptodome>=3.19.0

# ── MD4 fallback (some Python builds disable MD4 in OpenSSL) ─────────────────
# Only needed if hashlib.new("md4") raises ValueError
# pymd4>=0.1.0   # Uncomment only if needed

# ── Standard library modules used (no install needed) ────────────────────────
# argparse, hashlib, subprocess, struct, json, logging, os, re, tempfile
# All included in Python 3.8+

# ─────────────────────────────────────────────────────────────────────────────
# Optional: Enhanced output
# ─────────────────────────────────────────────────────────────────────────────
# colorama>=0.4.6   # Cross-platform ANSI colors (Windows terminal support)
